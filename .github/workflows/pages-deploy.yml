name: pages-deploy
on:
  push:
    branches: [main]
    paths:
      - "site/**"
      - "assets/**"
      - "templates/**"
      - "src/**"
permissions:
  pages: write
  id-token: write
jobs:
  deploy:
    uses: ./.github/workflows/deploy.yml
